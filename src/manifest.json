{
  "manifest_version": 3,
  "name": "__MSG_extName__",
  "description": "On windows, using alt + tab to switch between windows would cause youtube video unresponsive to space (normally it would toggle play and pause). Also pressing tab alone would cause this issue. Though it's supposed for accessibility purpose. This extension makes youtube page to always respond to space regardless of what state it is.",
  "permissions": ["storage"],
  "default_locale": "zh_TW",
  "action": {
    "default_popup": "popup.html",
    "default_icon": "icon-34.png"
  },
  "icons": {
    "128": "icon-128.png"
  },
  "content_scripts": [
    {
      "matches": ["*://*.youtube.com/*"],
      "js": ["contentScript.bundle.js"],
      "run_at": "document_end"
    }
  ],
  "web_accessible_resources": [
    {
      "resources": ["icon-128.png", "icon-34.png"],
      "matches": []
    }
  ]
}
